{
  "name": "tabs-2-json",
  "version": "2026.7.6",
  "description": "Browser extension that exports the text and metadata of selected tabs to JSON. This package.json exists only for the test suite; the extension itself has no build step and no runtime dependencies.",
  "private": true,
  "type": "module",
  "license": "CC-BY-NC-SA-4.0",
  "scripts": {
    "test": "node --test test/unit.mjs test/capture.test.mjs test/extractor.test.mjs test/pipeline.test.mjs test/holistic.test.mjs",
    "test:unit": "node --test test/unit.mjs",
    "test:capture": "node --test test/capture.test.mjs",
    "test:extractor": "node --test test/extractor.test.mjs",
    "test:pipeline": "node --test test/pipeline.test.mjs",
    "test:holistic": "node --test test/holistic.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
